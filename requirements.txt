# pinned to avoid the NumPy-2 / compiled-extension incompatibility
numpy<2
openai-whisper
sounddevice
certifi
tqdm
regex
requests

# NOTE: install torch separately following platform-specific instructions:
# https://pytorch.org/get-started/locally/
# Optionally add a line here like `torch` if you accept platform-specific pip behavior.

PyQt6
librosa
certifi